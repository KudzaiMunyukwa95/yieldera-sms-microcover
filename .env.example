# Africa's Talking Configuration
AT_USERNAME=your_at_username
AT_API_KEY=your_at_api_key

# Flask Backend Configuration
FLASK_BASE_URL=https://your-flask-backend.com/api

# Server Configuration
PORT=3000
NODE_ENV=development

# Optional: SMS Configuration
DEFAULT_SENDER_ID=YIELDERA
SMS_TIMEOUT=30000

# Optional: Rate Limiting (requests per minute per phone number)
RATE_LIMIT_PER_PHONE=10
```

## 🔧 **For Your Render Deployment, Set These Variables:**

### **Required:**
```
AT_USERNAME=sandbox
AT_API_KEY=your_africastalking_api_key
FLASK_BASE_URL=https://api.yieldera.com
NODE_ENV=production
```

### **Optional:**
```
PORT=3000
DEFAULT_SENDER_ID=YIELDERA
SMS_TIMEOUT=30000
